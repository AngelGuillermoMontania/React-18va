import React from "react";

export default function Cart() {
  return (
    <div>
      {
        // cart.map(elem => <Card id={elem.id} />)
      }

      <h1>
        {
          {
            /* TOTAL DE TODO EL CARRITO */
          }
        }
      </h1>
    </div>
  );
}
